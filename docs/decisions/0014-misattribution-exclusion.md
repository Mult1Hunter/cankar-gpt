# ADR 0014 - misattribution exclusion via NOT_BY_AUTHOR

**Status:** merged into ADR 0004, docs/decisions/0004-works-registry-as-source-of-truth.md (2026-07-26)

## Context

Its mechanism is a registry flag plus a merge-time drop - that is 0004's subject. The re-merge counts belong in registry/reports/merge.md, where they already are.

## Decision

Withdrawn as a standing record. The content lives at `docs/decisions/0004-works-registry-as-source-of-truth.md`; this number
is retained so existing `ADR 0014` citations still resolve.

## Consequences

Nothing changes mechanically - see ADR 0023 for the consolidation that produced
this stub, and `docs/decisions/README.md` for where each topic now lives.
