# ADR 0005 - stage subpackages for the Python monorepo side

**Status:** merged into ADR 0007, docs/decisions/0007-structure-law.md (2026-07-26)

## Context

Three of its four legs were already superseded by 0007; the surviving one (one package / one venv, uv-workspace split deferred) is a bullet there.

## Decision

Withdrawn as a standing record. The content lives at `docs/decisions/0007-structure-law.md`; this number
is retained so existing `ADR 0005` citations still resolve.

## Consequences

Nothing changes mechanically - see ADR 0023 for the consolidation that produced
this stub, and `docs/decisions/README.md` for where each topic now lives.
