# ADR 0008 - code architecture standards

**Status:** withdrawn - content moved, .claude/rules/code-standards.md (2026-07-26)

## Context

Conventions, not decisions, and duplicated five ways: the rules file, the AST test in tests/structure/test_layout.py, mypy and import-linter config in pyproject.toml, cankar/core/ itself, and the design-brief skill's trigger table.

## Decision

Withdrawn as a standing record. The content lives at `.claude/rules/code-standards.md`; this number
is retained so existing `ADR 0008` citations still resolve.

## Consequences

Nothing changes mechanically - see ADR 0023 for the consolidation that produced
this stub, and `docs/decisions/README.md` for where each topic now lives.
